{
  "name": "Finding",
  "type": "object",
  "properties": {
    "pca_record_id": {
      "type": "string"
    },
    "issue_description": {
      "type": "string"
    },
    "amount": {
      "type": "number"
    },
    "status": {
      "type": "string",
      "enum": [
        "Open",
        "Resolved",
        "Pending"
      ],
      "default": "Open"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "pca_record_id",
    "issue_description"
  ]
}